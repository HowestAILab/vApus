For a manual build:
Make sure there is a folder Prerequisites\vApus SMT containing vApusSMT.Base.dll and vApusSMT.Proxy.dll (from the vApus SMT Build folder).
Build using msbuild or visual studio
Copy the monitorsources folder from the vApus SMT Build folder to the vApus Build folder. (the folder, not just the files)